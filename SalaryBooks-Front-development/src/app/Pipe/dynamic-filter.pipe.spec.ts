import { DynamicFilterPipe } from './dynamic-filter.pipe';

describe('DynamicFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
